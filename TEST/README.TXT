sTSTCMOS was unable to store and retrieve NSTEPS. It always returned zero 
even when assembled and linked with MASM611.

TSTCMOS returns a value of C600h when run on my E-machine and zero on the 
vortex. I suspect that stst is the procedure that is not working.
